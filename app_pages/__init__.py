# app_pages/ — one module per app page. Each exposes a render() function
# that takes (fact, dim_well, dim_ingredient, state) and writes to Streamlit.
# Named app_pages (not pages) to avoid Streamlit's native multipage convention.
